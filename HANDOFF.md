# HANDOFF

Что не закончено прямо сейчас. Перезаписывается; незавершённое чужое не стирается.

**В работе:** этап «Прозрачность агрегации» — [docs/PLAN.md](docs/PLAN.md).

Свод правил и шесть документов проекта закоммичены в `main`, дерево чистое,
`npm run check` зелёный.

**Следующий шаг.** Вывести в сообщение прогноза значения по провайдерам и
предупреждение о расхождении:

1. Отдать наружу из `aggregateSourceSummaries` (`src/bot.js`) список источников после
   отбрасывания выбросов — переменная `kept`; сейчас она остаётся внутри функции.
2. Новый модуль `src/forecast-details.js` с чистым форматированием блока и тест
   `test/forecast-details.test.js` рядом: `src/bot.js` ничего не экспортирует.
3. В `buildForecast` приклеивать блок **после** `polishForecastWithLlm`, иначе валидатор
   редактуры будет отбрасывать ответ модели каждый раз, когда источники разошлись.
4. Для плановых поездок дальше семи дней — без предупреждения, но со значениями
   по провайдерам; расстояние по датам считать существующей `daysUntil`.
