# WORKLOG

## 2026-09-05 — сериализация saveStore

**План.** `pollOnce` и `sendScheduledForecasts` работают concurrently: обе
могут модифицировать `store` и вызвать `saveStore`, который пишет весь `store`
в JSON. Если две записи в один файл пересекутся, можно потерять изменения или
повредить файл.

**Сделано.** `saveStore` переписан: `JSON.stringify(store)` вызывается
синхронно (фиксирует текущее состояние), а сама запись сериализована через
promise chain (`saveChain`). Каждый вызов ждёт завершения предыдущей записи.
Ошибка предыдущей записи не ломает цепочку (`.catch(() => {})`).

**Проверено.** `npm run check`: `node --check` + 8 тестов, все зелёные.

Что делали. Новые записи сверху, старые не правятся.

## 2026-09-05 — починка документации

**План.** Убрать из документов утверждения, которые перестали быть верными.

**Сделано.** Ветка `claude/apply-coding-rules-b3rj5h` на origin больше не существует
(проверено через GitHub API: осталась только `main`). Из `STATUS` удалена запись о том,
что ветка осталась на origin из-за HTTP 403; вместо неё — что ветка удалена, содержимое
в `main`. Из `HANDOFF` удалён раздел «Мелочь, которую я не смог закрыть» целиком —
проблема закрыта. Дата в `STATUS` обновлена на 2026-09-05.

## 2026-09-04 — передача деплоя

**План.** Убрать ветку `claude/apply-coding-rules-b3rj5h` и оставить handoff агенту,
который имеет право деплоить.

**Сделано.** Локальная ветка удалена. Удалить её на origin не вышло: прокси окружения
отвечает HTTP 403 на удаление ветки — см. `JOURNAL`. `HANDOFF.md` переписан под
деплоящего агента: что лежит в `main` и не проверено на бою, почему не нужны миграции
и новые переменные, и как проверить новый блок детерминированно — плановой поездкой
на 8+ дней вперёд. `STATUS` согласован.

**Дальше.** Деплой и живая проверка — за Watson: владелец ответил, что деплой ручной,
что он сам смотрит сообщение в Telegram и продакшен после выкатки и что после закрытого
этапа агент ждёт его проверки. Ответы перенесены в `CLAUDE.md`, открытых вопросов там
больше нет. Сами команды деплоя по-прежнему не записаны — их вносит тот, кто выкатит.

## 2026-09-04 — прозрачность агрегации

**План.** Этап из [PLAN.md](PLAN.md): показать значения по источникам и предупреждать
о расхождении, для дальних поездок — без предупреждения.

**Сделано.**

- `aggregateSourceSummaries` отдаёт `sources` — список источников после отбрасывания
  выбросов; раньше он не выходил за пределы функции.
- Новый модуль `src/forecast-details.js`: собирает блок со значениями по источникам,
  выбирает заголовок (предупреждение или пометка про дальний прогноз) и пропускает окна,
  где остался один источник. Туда же переехала `formatTempRange` — без изменений,
  чтобы не заводить вторую копию форматирования.
- `buildForecast` приклеивает блок после `polishForecastWithLlm`. Признак дальней
  поездки — `daysUntil(date, timezone) > 7` для планового сценария.
- Шесть тестов в `test/forecast-details.test.js`. `npm run check` зелёный: 8 из 8.

**Дальше.** Этап закрыт, следующий за владельцем. Живой проверки блока в Telegram
не было — см. «Не проверялось» в [STATUS.md](STATUS.md).

## 2026-09-04 — свод правил, документы проекта, план по агрегации

**План.** Перенести канонический свод правил владельца
(`code-agent-43824/coding-rules/AGENTS.md`) в репозиторий, подключить его к Claude Code
и завести шесть документов проекта из §2 свода.

**Сделано.**

- `AGENTS.md` скопирован без изменений; расхождений с каноном нет.
- `CLAUDE.md` переписан: первой строкой импорт `@AGENTS.md`, дальше только проектное —
  команды, карта кода, порядок добавления погодного провайдера, принятые решения
  с причинами, отступление от свода, дисциплина версии, деплой. Правила из свода
  в нём не пересказываются (§0).
- Заведены `ROADMAP`, `PLAN`, `STATUS`, `WORKLOG`, `JOURNAL` и `HANDOFF`.
- При разборе кода найдено расхождение с `docs/weather-providers.md`: описанное там
  правило про предупреждение о расхождении источников и про поездки дальше семи дней
  в сообщение не выведено. Решение владельца — реализовать в коде; см. `JOURNAL`.

**Дальше.** Этап «Прозрачность агрегации» из [PLAN.md](PLAN.md): вывести значения
по провайдерам и предупреждение о расхождении, задействовать `daysUntil`, вынести
форматирование в отдельный модуль с тестами.
