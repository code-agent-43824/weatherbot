{
  "name": "weatherbot",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "scripts": {
    "start": "node src/bot.js",
    "check": "for f in src/*.js; do node --check \"$f\" || exit 1; done && node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
